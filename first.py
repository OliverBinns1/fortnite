name = input('what is your name?')
age = int(input(f'how old are you {name}?'))

print()
print(f"hello, {name} and you are {age} correct?") # an f is needed if you want the interpreter to do something to the string before printing such as taking an input and then printing it in a string
print()
print(f"you will be {age+1} in a year right?")



# the bit of code {name} calls the variable which is an input and so it takes an input from the user 

